from django.apps import AppConfig


class RequestOrdersConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'request_orders'
    verbose_name="จัดการข้อมูล Request Order"
